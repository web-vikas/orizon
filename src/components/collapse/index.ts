import { InternalCollapse } from "./Collapse";

const Collapse = InternalCollapse;

export { Collapse };
export type {
  CollapseProps,
  CollapseItem,
  CollapsibleType,
  ExpandIconPosition,
  CollapseSize,
} from "./types";
